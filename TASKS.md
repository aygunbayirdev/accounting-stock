# Accounting & Stock Management — Full-Stack Temizlik & Tamamlama Planı

**Amaç:** Şu an backend'i MVP seviyesinde, frontend'i büyük ölçüde eksik olan bu projeyi; **temiz kodlu, güvenli, uçtan uca ekranları tamamlanmış, portföyde gururla sergilenebilecek bir full-stack proje** haline getirmek.

**Kaynak:** Bu plan, backend (`Accounting.Api/Domain/Application/Infrastructure/Tests`) ve frontend (`accounting-web`) üzerinde yapılan tam kapsamlı bir kod incelemesinin bulgularına dayanıyor (2026-08-10). Mevcut kurallar `backend/PROJECT_RULES.md` ve `frontend/FRONTEND_RULES.md` dosyalarında tanımlı — bu plan o kuralları **değiştirmiyor**, onlara uyumu sağlıyor ve ihlal edilen yerleri düzeltiyor.

**Genel değerlendirme:** Backend'in mimari iskeleti (Clean Architecture + CQRS/MediatR + FluentValidation + JWT + audit trail + optimistic concurrency) sağlam — bu bir **yeniden yazım değil, disiplinli bir refactor + eksik parça tamamlama** işi. Frontend'de servis/model/interceptor/guard katmanı (18 servis, 20 model, auth altyapısı) beklenenden çok daha ileride; asıl eksik olan **ekranlar**. Yani önümüzdeki iş büyük ölçüde "var olan sağlam kalıpları tekrar tekrar uygulamak" — sıfırdan altyapı kurmak değil.

**Önerilen sıralama:** Önce backend'deki güvenlik/doğruluk açıklarını kapat (küçük ama kritik), sonra backend'i temizle, sonra frontend'i sistematik olarak inşa et (önce temel varlıklar — Cari/Şube/Depo — çünkü diğer ekranlar bunlara bağımlı), en son cilalama + test + deploy.

---

## Faz 0 — Kritik Güvenlik & Doğruluk Düzeltmeleri (Backend) ✅ TAMAMLANDI (2026-08-10)

Bunlar portföyde sergilemeden önce **mutlaka** kapatılması gereken, küçük ama gerçek açıklar.

- [x] `appsettings.json`'daki hardcoded zayıf JWT secret'ı kaldırıldı; `Program.cs` artık config'de secret yoksa/32 karakterden kısaysa startup'ta `InvalidOperationException` fırlatıyor. Lokal geliştirme için `dotnet user-secrets` kuruldu, `backend/README.md` güncellendi.
- [x] `GetStockByIdHandler` ve `GetStockMovementByIdHandler`'a `.ApplyBranchFilter()` eklendi (IDOR kapatıldı).
- [x] `UserConfiguration` ve `RoleConfiguration`'a `ApplySoftDelete()` eklendi; `Users.Email` unique index'i `HasFilter("[IsDeleted] = 0")` ile filtrelendi (migration: `AddUserRoleSoftDeleteFilters`). Not: `RolePermission`/`UserRole` saf junction entity — `ISoftDeletable` implemente etmiyorlar, bu yüzden onlara filter eklenmedi (ilk denetimin bu ikisi için önerisi yanlıştı, entity'ler incelenince düzeltildi).
- [x] `AuthController.Register` tamamen kaldırıldı (backend: controller/command/handler/validator; frontend: `/register` route, sayfası, `AuthService.register()`, login sayfasındaki link, interceptor whitelist girdisi). Karar: bu tek şirketli bir ERP, herkese açık self-registration yerine kullanıcılar `POST /api/users` (admin-only, şube+rol atamalı) ile oluşturuluyor — bu akış zaten mevcuttu ve register'dan daha güvenliydi.
- [x] `Program.cs`'deki migration+seed bloğu ayrıştırıldı: migration hâlâ koşulsuz çalışıyor (şema için gerekli), seed artık `Seeding:Enabled` config anahtarıyla korunuyor (varsayılan `true`, docker-compose'u bozmaz).
- [x] (Plan dışı, denetim sırasında bulundu) `backend/.dockerignore` ve `frontend/.dockerignore` eklendi — bunlar olmadan lokal `dotnet build`/`npm install` sonrası `docker compose build` host'un Windows'a özgü `obj/`/`node_modules` klasörlerini container'a kopyalayıp restore'u kırıyordu.
- [x] Doğrulama: `dotnet build` (0 hata), `dotnet test` (Faz 0 sonunda 75/75 yeşil), `npx tsc --noEmit` (frontend, 0 hata), `docker compose up -d --build sqlserver backend` + gerçek smoke test (health 200, seeded admin ile login 200, `/api/auth/register` artık 404, `docker compose build frontend` başarılı).
- [ ] **Commit edilmedi** — kullanıcı döndüğünde `git status`/`git diff` ile gözden geçirip onaylaması bekleniyor.

## Faz 1 — Backend Mimari Temizliği & Tutarlılık (kısmen tamamlandı, 2026-08-10)

Küçük/orta ölçekli maddeler ve (canlı testte ortaya çıkan) iki kritik bug otomatik pilotta tamamlandı ve doğrulandı (`dotnet build` 0 hata, `dotnet test` 75/75 yeşil, `docker compose` ile gerçek API smoke testi). Stok tutarlılığı sorunu başlangıçta "büyük mimari karar" diye ertelenmişti, ama N+1 düzeltmesini canlıda test ederken gerçek/üretimi bloke eden bir bug olduğu kanıtlandığı için ertelenmeden düzeltildi. Kalan büyük yüzeyli maddeler (API contract katmanı, ExceptionHandler geçişi, tam route taraması, hata mesajı dili) hâlâ **bilinçli olarak ertelendi**.

- [x] Ölü kod silindi: `GetInvoicesQueryHandler_EXAMPLE.cs`, `InvoiceCalculator.cs`, `CreateInvoiceRequest.cs`/`CreateInvoiceResponse.cs`, `IAppDbContext.QueryRaw`/`AppDbContext.QueryRaw`, `UnitTest1.cs`, `Permissions.cs`'deki `ExpenseList`/`ExpenseDefinition`/`FixedAsset` grupları + `DataSeeder.cs`'deki bunlara referans veren rol-izin atamaları.
- [x] **Fatura toplam hesaplaması birleştirildi**: yeni `Accounting.Application/Services/InvoiceLineCalculator.cs`, hem `CreateInvoiceHandler` hem `UpdateInvoiceHandler` artık bunu kullanıyor. Bu arada gerçek bir tutarsızlık bulundu ve düzeltildi: `UpdateInvoiceHandler.ProcessLine` satır tutarını `RoundQuantity` (3 hane) ile yuvarlıyordu, `CreateInvoiceHandler` ise `RoundAmount` (2 hane) kullanıyordu — aynı fatura önce oluşturulup sonra güncellenirse `Gross`/`Net`/`Vat` küçük farklarla kayabiliyordu. Artık ikisi de `RoundAmount` kullanıyor (PROJECT_RULES.md'nin `AmountJsonConverter` 2-hane kuralıyla tutarlı).
- [x] `TransferStockHandler`'daki kendi kendine bırakılmış "Wait, ... tutarsız mı?" yorumu incelendi: **gerçek bir tutarsızlık yoktu** — hem `TransferStockHandler` hem `CreateStockMovementHandler` zaten `StockMovement.Quantity`'yi pozitif büyüklük olarak saklıyor, yön `Type` ile belirleniyor. Kafa karıştıran yorum, kararı netleştiren bir yorumla değiştirildi.
- [x] `BranchesController`'ın block-scoped namespace'i diğer 17 controller gibi file-scoped'a çevrildi.
- [x] JWT settings'in çift bağlanması giderildi: `Program.cs`'deki `AddSingleton(Options.Create(jwtSettings))` kaldırıldı; DI'daki tek gerçek kaynak artık `Infrastructure/DependencyInjection.cs`'deki `services.Configure<JwtSettings>(...)`. `Program.cs`'deki yerel `jwtSettings` değişkeni sadece `JwtBearerOptions` kurulumu için kullanılıyor (açıklayıcı yorum eklendi).
- [x] CORS origin'leri `Cors:AllowedOrigins` config bölümünden okunuyor (`Cors__AllowedOrigins__0` env var ile prod origin eklenebilir), config yoksa mevcut localhost:3000/4200 varsayılanına düşüyor — geriye dönük uyumlu.
- [x] **`ApproveOrderHandler` N+1 düzeltildi**: satır başına `ValidateStockAvailabilityAsync` yerine tek sorgulu `ValidateBatchStockAvailabilityAsync` kullanılıyor. `OrderTests.cs`'e gerçek bir `Verify()` eklendi (eskiden loose mock sayesinde hiçbir şeyi doğrulamadan geçiyordu).
- [x] **`CreateInvoiceHandler` stok hareketi N+1 düzeltildi**: satır başına iç içe `_mediator.Send(CreateStockMovementCommand)` (N ayrı dispatch + N ayrı SaveChanges) kaldırıldı; artık tüm satırlar için tek sorgu ile mevcut `Stock` satırları çekiliyor, hesaplama bellekte yapılıyor, tek `SaveChangesAsync` ile kaydediliyor. Ortak "stok yönü + negatife düşme kontrolü" mantığı yeni `StockQuantityCalculator`'a çıkarıldı, hem burada hem `CreateStockMovementHandler`'da kullanılıyor (kopya kod/kayma riski önlendi). Artık kullanılmayan `IMediator` bağımlılığı `CreateInvoiceHandler`'dan kaldırıldı.
- [x] **Stok tutarlılığı sorunu çözüldü** (önceden "büyük mimari karar, ertelendi" olarak işaretliydi — N+1 düzeltmesini canlıda docker-compose ile test ederken **gerçek, üretimi bloke eden bir bug olduğu kanıtlandı**, bu yüzden ertelenmeden düzeltildi): `StockService.GetStockStatusAsync` artık müsaitliği `Stock` tablosundaki fiziksel bakiyeden (branch-scoped, `ApplyBranchFilter` ile) hesaplıyor, eskisi gibi fatura/sipariş satırlarını yeniden toplamıyor. `QuantityIn`/`QuantityOut` bilgilendirme amaçlı fatura-türetilmiş toplamlar olarak kaldı (raporlarda kullanılıyor), ama `QuantityAvailable` (tüm satış/onay validasyonlarının dayandığı tek alan) artık gerçek stoktan hesaplanıyor. **Canlıda kanıtlanan gerçek etki**: seed verisindeki "Açılış stoku" gibi doğrudan stok hareketiyle girilen miktarlar eski hesaplamada hiç sayılmıyordu — gerçek stok 62 iken sistem "-4 mevcut" diyip her satışı reddediyordu. `StockServiceTests.cs`'e bunu kanıtlayan bir regresyon testi eklendi (`ValidateStockAvailability_ShouldSucceed_ForStockWithNoPurchaseInvoice`).
- [x] **(Plan dışı, bu sırada bulunan ayrı bir kritik bug)** `CreateInvoiceValidator`/`UpdateInvoiceValidator`'daki `AllItemsBelongToSameBranchAsync`, Item'ların global (branch-agnostic) yapıldığı migration'dan sonra ters mantıkla kalmıştı: `!mismatchedItems` aslında "bu ID'lerden herhangi biri DB'de var mı" sorusuna bakıyordu ve varsa validasyonu **başarısız** kılıyordu — yani gerçek bir ürünle fatura oluşturmak/güncellemek her zaman 400 dönüyordu. `AllItemsExistAsync`'e çevrildi (gerçek varlık kontrolü yapıyor, olmayan/silinmiş ID'lerde anlamlı hata veriyor). Docker-compose üzerinden gerçek bir Purchase + Sales faturası uçtan uca oluşturularak doğrulandı.
- [ ] **API contract (Request/Response DTO) katmanı (ERTELENDİ)**: 18 controller'ın tamamını kapsayan, public API şeklini değiştiren geniş yüzeyli bir değişiklik — tek oturumda mekanik yapılırsa gözden kaçan bir kırılma riski yüksek.
- [ ] **`ExceptionToProblemDetailsMiddleware` → yerleşik `IExceptionHandler` (ERTELENDİ)**: Hata response şeklini (ProblemDetails alanları) etkileyebilecek bir değişiklik, frontend'in `http-problem-interceptor.ts`'i buna göre yazıldığı için birlikte test edilmeden yapılmamalı.
- [ ] Route pattern / `{id:int}` constraint / RowVersion body-vs-query tutarsızlıklarının tam taranması (ERTELENDİ — 18 controller'ı tek tek gözden geçirmek gerekiyor, BranchesController'daki namespace dışında dokunulmadı).
- [ ] Hata mesajı dili tutarlılığı (Türkçe/İngilizce karışık) (ERTELENDİ — büyük string-literal taraması gerektiriyor, ayrı bir oturumda yapılmalı).

## Faz 2 — Backend Test Kapsamının Genişletilmesi (kısmen tamamlandı, 2026-08-10)

- [x] `Accounting.Tests` projesine `Microsoft.AspNetCore.Mvc.Testing` + `Accounting.Api` referansı eklendi. `Integration/CustomWebApplicationFactory.cs`: gerçek `Program`'ı (`Testing` ortamında) ayağa kaldırıyor, `AppDbContext`'i EF Core InMemory ile değiştiriyor, migration+seed'i atlıyor (`Program.cs`'e `!app.Environment.IsEnvironment("Testing")` guard'ı eklendi — InMemory provider `MigrateAsync` desteklemiyor). `WebApplicationFactory<Program>` çalışabilsin diye `Program.cs`'in sonuna `public partial class Program {}` eklendi (top-level statement'ların örtük ürettiği sınıfı dışarıdan erişilebilir yapıyor). JWT secret gibi ayarlar `Environment.SetEnvironmentVariable` ile veriliyor (config-injection zamanlaması `builder.Build()` öncesi okunan değerlere yetişmiyor, ortam değişkenleri senkron okunduğu için güvenilir tek yol bu oldu).
- [x] İlk entegrasyon testleri yazıldı (`Integration/AuthEndpointsIntegrationTests.cs`, `Integration/BranchIsolationIntegrationTests.cs`): gerçek login akışı, `register` 404, yanlış şifre, token'sız 401 — ve **Faz 0'da düzeltilen iki branch-isolation açığı için gerçek HTTP seviyesinde regresyon testi**: başka şubenin stok/stok hareketi kaydına gerçek bir JWT ile 404 döndüğü doğrulanıyor (önceki unit testler bunu hiç test etmiyordu, çünkü handler'ları doğrudan çağırıyorlardı — branch filter'ın asıl riski HTTP+auth katmanında).
- [ ] `ExceptionToProblemDetailsMiddleware`'in 6 dalını kapsayan entegrasyon testleri (ERTELENDİ — daha fazla controller/senaryo gerektiriyor, sonraki bir oturuma bırakıldı).
- [x] ~~`AuthController.Register`'ın yetkilendirme/rol atama davranışını test et~~ — madde geçersizleşti, Faz 0'da endpoint tamamen kaldırıldı; kaldırıldığını doğrulayan `Register_Endpoint_ShouldNotExist` testi eklendi.
- [x] Stok tutarlılığı düzeltmesi (Faz 1) için: manuel/açılış stok düzeltmesi sonrası `StockService`'in doğru müsaitlik döndürdüğünü doğrulayan test eklendi (`StockServiceTests.ValidateStockAvailability_ShouldSucceed_ForStockWithNoPurchaseInvoice`) — canlıda bulunan gerçek bug'ı kanıtlıyor.
- [x] ~~`GetStockByIdHandler`/`GetStockMovementByIdHandler`'daki branch-isolation düzeltmesi için özel bir regresyon testi yok~~ — `BranchIsolationIntegrationTests`'te eklendi (bkz. yukarı).
- [ ] Kalan controller'lar için daha fazla entegrasyon testi (payment/cheque akışları uçtan uca) — genişletilebilir, şimdilik altyapı + en kritik birkaçı kuruldu.

## Canlı Test Turu — Diğer Ana Akışlar (2026-08-10)

Faz 0/1/2 sonrası, aynı yöntemle (docker-compose + gerçek `curl` istekleri) sipariş, ödeme ve çek akışları da uçtan uca denendi. **İki kritik bug daha bulundu ve düzeltildi** — ikisi de yine `dotnet test`'in hiç yakalamadığı, sadece gerçek isteklerle ortaya çıkan türden:

- [x] **Sipariş numarası çakışması (üretimi bloke ediyordu)**: `CreateOrderHandler`, aynı şube+tür için "son siparişi" bulurken tüm `OrderNumber` string'lerini büyükten küçüğe sıralayıp en büyüğünü sayıya çevirmeye çalışıyordu. `DataSeeder` siparişleri `"SO-2026-0001"`/`"PO-2026-0001"` formatında seed ediyor; bu format hem string sıralamada `"000001"`'den önce gelir (harf > rakam) hem de sayıya çevrilemez — sonuç: numara üretici her seferinde sessizce 1'e sıfırlanıyordu. Aynı şube+tür için **ikinci gerçek sipariş her zaman** `IX_Orders_...OrderNumber` unique index'ine çarpıp 500 dönüyordu. Düzeltme: numara üretimi artık `InvoiceNumberService`'teki gibi kendi prefix'ine göre (`StartsWith`) filtreleyip sıralıyor, format seed verisiyle aynı aileye (`SO-2026-0001` tarzı) çekildi. Regresyon testi: `OrderTests.CreateOrder_TwiceForSameBranchAndType_ShouldNotReuseOrderNumber`.
- [x] **Sipariş→fatura dönüşümünde yanlış tutarlar + eksik negatif-stok koruması**: `CreateInvoiceFromOrderHandler`, satır `Gross` alanına (dokümante edilmiş anlamı: "Qty × Fiyat, iskonto öncesi") yanlışlıkla `Net + KDV` yazıyordu, `GrandTotal` hiç set edilmiyordu (0 kalıyordu), ve fatura başlığındaki `TotalLineGross` da hiç hesaplanmıyordu (0 kalıyordu — canlıda `"totalLineGross":"0.00"` olarak görüldü). Ayrıca stok güncellemesi elle yazılmıştı, `StockQuantityCalculator`'ın negatif-stok korumasından geçmiyordu, ve sadece `ItemId` doluluğuna bakıyordu — `ItemType.Inventory` kontrolü yoktu (Hizmet/Masraf/Demirbaş kalemleri için de yanlışlıkla stok hareketi oluşturuyordu). Düzeltme: satır hesaplaması `InvoiceLineCalculator`'a, stok güncellemesi `StockQuantityCalculator`'a yönlendirildi, Inventory-tipi filtresi eklendi. Regresyon testi: `OrderToInvoiceConversionTests.CreateInvoiceFromOrder_ShouldProduceCorrectLineAndHeaderTotals` (daha önce bu handler'ı test eden **hiçbir** test yoktu).
- [x] Doğrulama: `dotnet build` (0 hata), `dotnet test` (83/83), ve docker-compose üzerinde gerçek uçtan uca akış: sipariş oluştur → onayla → faturaya çevir (doğru `totalLineGross` ile) → ödeme al (fatura bakiyesi 0'a düştü, kasa bakiyesi arttı) → çek oluştur → dashboard/gelir-gider raporları hatasız döndü. İki ardışık sipariş artık çakışmadan oluşuyor.
- [x] **Ürün (Item) oluşturma/güncelleme her zaman 400 dönüyordu** (cari/depo/kategori oluşturma turu sırasında bulundu): `CreateItemCommand`/`UpdateItemCommand`'daki `Type` alanı `ItemType` yerine düz `int` olarak tanımlanmıştı (PROJECT_RULES.md'nin kendi "Command/Query DTO'larında native enum kullan" kuralının ihlali — `CreateInvoiceCommand.Type` doğru şekilde `InvoiceType` kullanıyor, Item'lar istisnaydı), validator'lar ise `RuleFor(x => x.Type).IsInEnum()` çağırıyordu. `IsInEnum()` bir `int` üzerinde çalıştırılınca (gerçek bir enum tipi değil) her zaman geçersiz sonuç veriyor — yani **hangi değer gönderilirse gönderilsin** `POST`/`PUT /api/items` validasyonu başarısız oluyordu. Seed'deki 19 ürün DataSeeder tarafından bu API'yi hiç kullanmadan direkt eklendiği için sorun fark edilmemişti. Düzeltme: her iki command'da `Type` gerçek `ItemType` enum'una çevrildi, handler'lardaki gereksiz `(ItemType)` cast'leri kaldırıldı, validator'lardaki `(int)ItemType.FixedAsset` karşılaştırmaları `ItemType.FixedAsset`'e sadeleştirildi. Regresyon testi: `Integration/ItemEndpointsIntegrationTests.CreateItem_WithValidInventoryType_ShouldReturn200` (gerçek HTTP+validator zincirinden geçen ilk test — handler-seviyeli eski testler bu bug'ı hiç görmüyordu).
- [x] Doğrulama: `dotnet build` (0 hata), `dotnet test` (84/84), docker-compose üzerinde gerçek `POST /api/items` isteğiyle 201 doğrulandı. Cari (Contact), Depo (Warehouse), Kategori (Category) oluşturma akışları ayrıca denendi, sorun bulunmadı.
- [x] **Aynı `int` + `.IsInEnum()` kalıbının başka yerlerde tekrarlanıp tekrarlanmadığı statik olarak tarandı** (`grep IsInEnum`, 12 dosya): `Payment.Direction`, `CashBankAccount.Type`, `Order.Type`, `StockMovement.Type` — hepsi zaten doğru şekilde gerçek enum tipini kullanıyor. Item'lardaki bug izole bir örnekti, sistemik değil.
- [x] **Son doğrulama — tamamen temiz veritabanı**: `docker compose down -v` (mssql-data volume'ü tamamen silindi) ardından `docker compose up -d --build` ile sıfırdan başlatıldı; migration'lar + seed sıfır DB'ye karşı sorunsuz çalıştı, login çalıştı, iki ardışık sipariş çakışmadan oluştu (`SO-2026-0005`, `SO-2026-0006`), yeni ürün oluşturma 201 döndü. Bu, projeyi ilk klonlayacak/deploy edecek birinin göreceği tam senaryo — tüm oturum boyunca birikmiş bir veritabanına karşı değil, gerçekten sıfırdan doğrulandı.

## Faz 3 — Frontend: Bug Düzeltmeleri & Altyapı Temizliği (kısmen tamamlandı, 2026-08-10)

Yeni ekran yazmadan önce mevcut kodun sağlam bir temel olduğundan emin ol.

- [x] **Gerçek bug düzeltildi**: `invoice-edit.page.ts`'deki `(InvoiceFormComponent as any).prototype.id = this.id` satırı kaldırıldı. `InvoiceFormComponent`'e `private _id?: number` alanı eklendi, `@Input() set value(...)` setter'ı artık `v.id`'yi oraya atıyor, `onSave()`'deki `id: (this as any).id` de `id: this._id!`'ye çevrildi.
- [x] **(Plan dışı, bunu tarayıcıda test ederken bulunan 3 kritik bug daha, hepsi düzeltildi)**:
  1. **Fatura formu `type` alanını yanlış formatta gönderiyordu**: `onSave()` hem insert hem update için `h.type`'ı (form'un dahili string temsili, örn `'Sales'`) doğrudan backend'e gönderiyordu; backend'de `InvoiceType` için `JsonStringEnumConverter` kayıtlı değil, sayısal enum bekliyor. Sonuç: **UI üzerinden fatura oluşturma/güncelleme her zaman 400 dönüyordu** — invoice ekranı frontend'in en gelişmiş sayfası olmasına rağmen, kullanıcı arayüzünden gerçekte hiç kullanılamıyordu. Yeni `typeToNumber()` metodu eklendi (`normalizeType()`'ın tersi), her iki emit noktasında kullanılıyor.
  2. **Fatura validasyonu admin/HQ bypass'ını yoksayıyordu**: `CreateInvoiceValidator`/`UpdateInvoiceValidator`'daki `ContactBelongsToSameBranchAsync`, cari'nin şubesini doğrudan `_currentUserService.BranchId` (çağıranın KENDİ şubesi) ile karşılaştırıyordu — `ApplyBranchFilter`'ın her yerde uyguladığı admin/HQ bypass'ı yoktu. Sonuç: bir admin/HQ kullanıcısı **kendi "ev" şubesi dışındaki hiçbir cariye fatura kesemiyor/güncelleyemiyordu** — tam olarak merkez ofisin en çok ihtiyaç duyduğu senaryo. Aynı kalıp statik taramayla bulundu ve `CreateOrderValidator`, `CreateChequeValidator`, `CreatePaymentValidator` (2 yerde) için de düzeltildi (toplam 6 validator).
  3. **Fatura güncellemede stok senkronizasyonu bozuktu**: `UpdateInvoiceHandler.SyncStockMovements` eski stok hareketlerini soft-delete ediyordu ama `Stock.Quantity`'deki eski etkilerini **hiç geri almıyordu**, sonra `CreateStockMovementHandler`'ı mediator ile çağırıyordu — o da depoyu `invoice.BranchId`'ye değil, **çağıranın kendi şubesine** göre arıyordu. Sonuç: (a) bir faturanın satır miktarı değiştirildiğinde stok üst üste binerek bozuluyordu (5 adet düşülmüşken 3'e güncellenince toplamda 8 düşüyordu, net 3 değil), (b) admin/HQ başka şubenin faturasını güncellediğinde "Warehouse bulunamadı" hatası alıyordu. `CreateInvoiceHandler`/`CreateInvoiceFromOrderHandler`'da zaten kullanılan `StockQuantityCalculator` + tek-sorgu/tek-save deseniyle yeniden yazıldı, artık eski hareketlerin etkisini doğru şekilde geri alıp `invoice.BranchId`'ye göre uyguluyor. Artık kullanılmayan `IMediator` bağımlılığı `UpdateInvoiceHandler`'dan kaldırıldı.

  Bu üçü, fatura düzenleme ekranını (frontend'in en gelişmiş sayfası) uçtan uca gerçekten test edene kadar fark edilmemişti — regresyon testleri: `InvoicesTests.UpdateInvoice_ChangingLineQuantity_ShouldNetAdjustStock_NotStack`, `Integration/InvoiceCrossBranchIntegrationTests.CreateInvoice_ForContactInAnotherBranch_ShouldSucceed_WhenCallerIsHeadquarters`. Ayrıca `CreateInvoiceHandler`/`UpdateInvoiceHandler`'daki depo-arama sırası düzeltildi: artık depo kontrolü, faturada gerçekten Inventory tipinde satır olup olmadığından SONRA yapılıyor (sadece Hizmet/Masraf içeren bir fatura, o şubede depo tanımlı olmasa bile artık hata vermiyor).
  Doğrulama: `dotnet build` (0 hata), `dotnet test` (86/86), `npx tsc --noEmit` (0 hata), ve docker-compose + gerçek tarayıcı (Chrome DevTools üzerinden network request'leri izlenerek) ile: mevcut bir faturayı düzenleyip kaydetme (200), farklı bir faturaya SPA içi geçiş yapıp kaydetme (id sızıntısı yok), **ve yepyeni bir fatura oluşturma (201)** — üçü de artık gerçekten çalışıyor.
- [x] `app.spec.ts` düzeltildi: olmayan `./app`/`App`'i import ediyordu, gerçek kök component `AppComponent`'e göre yeniden yazıldı (`provideRouter([])`, `provideHttpClient()`, `provideHttpClientTesting()` ile). `ng test --watch=false --browsers=ChromeHeadless` → 2/2 yeşil (önceden derlenmiyordu bile).
- [x] `core/utils/money.utils.ts` artık gerçekten kullanılıyor: `invoices-form.component.ts`'deki elle yazılmış `preview()` (Decimal.js ile Net/KDV/Brüt hesabı) kaldırıldı, yerine `calculateInvoiceLine()` çağrısı geçti (iskonto/tevkifat bu formda henüz olmadığı için varsayılan 0 ile aynı sonucu veriyor — tarayıcıda 3×50, %18 KDV → Net 150.00, KDV 27.00 ile doğrulandı). Dosyanın kendi ayrı `Decimal.set(...)` çağrısı da kaldırıldı (`money.utils.ts` zaten global olarak aynı ayarı yapıyor).
- [x] ~~`adminGuard`'ı route'lara bağla~~ — şu an bağlanacak gerçek bir admin-only route yok (Users/Roles ekranları Faz 6'da yapılacak); guard doğru yazılmış halde bekliyor, o ekranlar gelince bağlanacak.
- [x] `src/styles.scss`'deki çift `@include mat.theme(...)` çağrısı tek bloğa indirildi (`density: -2` — ikinci blok zaten kazanıyordu, birincisi ölü koddu).
- [x] AG Grid tema kullanımı tekilleştirildi: `invoices-form.component.ts`'deki `ag-theme-quartz` CSS class'ı ve elle set edilen `[rowHeight]`/`[headerHeight]` input'ları kaldırıldı, paylaşılan `AG_THEME` (`core/ag-grid/ag-theme.ts`, `ListGridComponent`'in kullandığıyla aynı) `[theme]` binding'i ile bağlandı.
- [x] Fragile route-mode tespiti düzeltildi: `app.routes.ts`'deki üç invoice route'una (`invoices/new`, `invoices/:id/edit`, `invoices/:id`) açık `data: { mode: 'insert'|'edit'|'view' }` eklendi; `invoice-edit.page.ts` artık `this.router.url.endsWith('/edit')` yerine `this.route.snapshot.data['mode']` okuyor. Tarayıcıda her üç mod da doğrulandı (view: Kaydet butonu yok, edit: var ve kaydetme 200 dönüyor).
- [x] ~~`login-page.component.ts`/`register-page.component.ts`'deki neredeyse birebir kopya inline stilleri...~~ — madde geçersizleşti, `register-page.component.ts` Faz 0'da tamamen kaldırıldı.
- [ ] Hardcoded Türkçe UI string'lerini (hata mesajları, snackbar metinleri gibi tekrar edenleri) tek bir mesaj sabitleri dosyasında topla — tam i18n gerekmez, ama tekrarı ve tutarsızlığı önle (ERTELENDİ — büyük string taraması gerektiriyor, ayrı bir oturuma bırakıldı).

**Faz 3 durumu: hemen hemen tamamlandı** (son madde hariç, düşük öncelikli ve geniş kapsamlı olduğu için ertelendi). Doğrulama: `dotnet build`/`test` etkilenmedi (sadece frontend), `npx tsc --noEmit` (0 hata), `ng test --watch=false` (2/2), gerçek tarayıcı ile invoice new/edit/view akışları.

## Faz 4 — Frontend: Temel Varlık Ekranları (diğer ekranların önkoşulu) ✅ TAMAMLANDI (2026-08-10)

Fatura/sipariş formları şu an cari/şube ID'sini elle yazdırıyor — bu üç modül olmadan diğer ekranlar da tam olgunlaşamaz.

- [x] **Cariler (Contacts)** — liste + filtre (arama, şube, tür) + oluştur/düzenle/sil dialog'u (`features/contacts/`). Şirket/Şahıs toggle'ı, tüm flag'ler (Müşteri/Tedarikçi/Personel/Perakende), IBAN/e-posta/telefon dahil.
- [x] **Şubeler (Branches)** — liste + oluştur/düzenle/sil dialog'u (`features/branches/`).
- [x] **Depolar (Warehouses)** — liste + şube filtresi + oluştur/düzenle/sil dialog'u (`features/warehouses/`), varsayılan depo toggle'ı dahil.
- [x] Paylaşılan **`ConfirmDialogComponent`** (`shared/confirm-dialog/`) — tüm sil işlemlerinde kullanılan ortak onay dialog'u.
- [x] Paylaşılan **`EntityPickerComponent`** (`shared/entity-picker/`) — autocomplete arama bileşeni yazıldı ve derleniyor, ancak henüz hiçbir ekrana bağlanmadı (şubeler az sayıda olduğu için Warehouses/Contacts formlarında basit `mat-select` kullanıldı). Gerçek kullanım alanı Faz 5'teki invoice formunun `contactId`/`itemId` alanları — orada dogfooding edilip doğrulanmalı.
- [x] Paylaşılan **`EntityActionsCell`** (ag-grid cellRenderer, `shared/list-grid/`) — dialog tabanlı ekranlarda Düzenle/Sil ikonları için; `ListGridComponent`'e `context` Input'u eklendi.

**Bu fazda canlı test sırasında bulunan ve düzeltilen kritik bug'lar:**
- **AG Grid cellRenderer'lar hiçbir zaman render olmuyordu (uygulama genelinde, sadece yeni ekranlarda değil)** — kök neden: ilk render'daki cellRenderer oluşturma AG Grid tarafından `requestAnimationFrame`'e erteleniyor; bu otomasyon/headless ortamında (ve gerçek kullanıcıda sekme arka plandaysa) hiç tetiklenmeyebiliyor, hücre sonsuza kadar boş kalıyor. `gridOptions.suppressAnimationFrame: true` ile düzeltildi (`shared/list-grid/list-grid.component.ts` ve `invoices-form.component.ts`). Bu, faturalar listesindeki Görüntüle/Düzenle ikonlarının ve fatura satır grid'indeki Sil butonunun da bu oturumdan önce **hiç çalışmadığını** ortaya çıkardı — muhtemelen o ekranlar hep URL'e direkt gidilerek test edilmiş, ikonlara hiç tıklanmamış. Bonus olarak o da düzeltildi.
- **Actions kolonları `field: 'id'`'i ID kolonuyla paylaşıyordu** — AG Grid'de aynı `field`'a sahip iki kolon tanımlandığında ikincinin `cellRenderer`'ı sessizce yok sayılıyor. Tüm actions kolonları artık `field` yerine benzersiz `colId: 'actions'` kullanıyor (branches/warehouses/contacts/invoices-page).
- **`BranchDto`/`WarehouseDto`/`ContactDto` liste (list item) DTO'ları backend'de `RowVersion` içermiyor** (sadece detail DTO'lar içeriyor) — ama frontend tipleri (`BranchListItemDto = BranchDto` gibi) bunu varsayıyordu, düzenleme/silme `rowVersion: undefined` gönderip 400 alıyordu. Üç modülün `ListItemDto` tipleri gerçek backend şekline göre ayrıştırıldı; Branches/Warehouses/Contacts'ın edit/delete akışları artık `getById()` ile güncel veriyi çekip kullanıyor.
- **`UpdateBranchBody`/`UpdateWarehouseBody`/`UpdateContactBody`'nin rowVersion alan adı backend'le uyuşmuyordu**: Branch için frontend `rowVersionBase64` diyordu ama backend `RowVersion` bekliyordu; Warehouse/Contact için ise tam tersi (frontend `rowVersionBase64`, backend `RowVersion`). Üçü de backend'in gerçek `UpdateXCommand` alan adına göre düzeltildi.
- `ContactsService.list()` yanlış response tipi (`{totalCount, items}` yerine gerçek `PagedResult<T>` şekli `{total, items, ...}`) ve yanlış query param adı (`page` yerine `pageNumber`) kullanıyordu.
- `WarehousesService`/`ContactsService`'in `page` query param'ı backend'in beklediği `pageNumber`'dan farklıydı.
- `ListWarehousesQuery.BranchId` backend handler'da hiç kullanılmıyordu (ölü filtre) — şimdi `ApplyBranchFilter`'dan sonra ek `Where` ile gerçekten filtreliyor.

**Bilinçli olarak ertelenen:** `EntityPickerComponent` henüz gerçek bir ekranda kullanılmıyor (yukarıda açıklandı) — Faz 5'te invoice formunun contact/item seçiminde dogfooding edilmeli.

## Faz 5 — Frontend: Mevcut Ekranların Tamamlanması

- [x] **Fatura formu** ✅ TAMAMLANDI (2026-08-10) — `contactId` artık Faz 4'teki `EntityPickerComponent` ile (arama + CUST001 gibi kod/ad gösterimi); satır ekleme de aynı şekilde bir "Ürün Ekle" entity-picker'ı ile (seçilince `ItemsService.getById` çağrılıp kod/ad/birim/KDV/satış fiyatı otomatik dolduruluyor — eski akışta sadece çıplak bir `itemId` sayısı vardı). Eksik alanlar eklendi: `discountRate`/`withholdingRate` (satır bazlı, editable grid kolonları), `documentType`, `waybillNumber`, `waybillDateUtc`, `paymentDueDateUtc` (header). Satır grid'ine validasyon eklendi (`validateLines()`): en az bir satır, her satırda geçerli `itemId`, `qty>0`, `unitPrice>=0`, `vatRate`/`discountRate`/`withholdingRate` 0-100 aralığında — aksi halde kaydetmeden önce hata mesajı gösteriliyor.
  - **Keşfedilen gerçek bug**: form'daki "Şube (ID)" alanı tamamen ölüydü — `CreateInvoiceCommand`/`UpdateInvoiceCommand`'da hiç `BranchId` alanı yok; backend her zaman `ICurrentUserService.BranchId`'yi (çağıranın kendi şubesi) kullanıyor. Kullanıcı ne yazarsa yazsın sessizce yok sayılıyordu. Zorunlu/editable input kaldırıldı, yerine salt-okunur "Şube: KOD - Ad" bilgisi (edit/view) veya "kaydettiğinizde oturum açan kullanıcının şubesine atanır" ipucu (insert) geldi.
  - **Keşfedilen ikinci bug**: `money.utils.ts`'deki `calculateInvoiceLine()` (satır önizleme hesaplaması) backend'in gerçek `InvoiceLineCalculator.Calculate`'inden (Accounting.Application/Services/InvoiceLineCalculator.cs) sessizce sapmıştı — frontend'de "gross" = Net+KDV anlamına gelirken backend'de `Gross` = Qty×UnitPrice (iskonto ÖNCESİ ham tutar); frontend "grandTotal"ı tevkifatı düşerken backend `GrandTotal` = Net+KDV'yi hiç düşmüyor. Önizleme değerleri (özellikle iskonto/tevkifat eklenince) kaydedilecek gerçek değerlerden farklı görünürdü. `calculateInvoiceLine` backend'le birebir eşleşecek şekilde yeniden yazıldı; grid'deki "Brüt" kolonu kaldırılıp anlamı netleşen "Genel Toplam" (Net+KDV) ile değiştirildi. Canlı testte (satır oluştur → %10 iskonto uygula → kaydet) önizlemenin gösterdiği Net=13500.00/KDV=2700.00 ile backend'den dönen değerler birebir eşleşti.
  - Docker Compose ile canlı doğrulandı: cari seçimi, ürün-seç-ile-satır-ekleme, iskonto hesaplaması, ve güncelleme akışının tamamı gerçek tarayıcıda uçtan uca test edildi (POST 201, PUT 200, GET ile persisted değerler doğrulandı).
- [x] **Stok kartları (Items)** ✅ TAMAMLANDI (2026-08-10) — `ItemFormDialogComponent` (yeni) ile oluştur/düzenle akışı, listeye "Yeni Kart" butonu + `EntityActionsCell` (Düzenle/Sil) + "Tür" kolonu eklendi. Backend kaynağı (`CreateItemCommand.cs`, `UpdateItemCommand.cs`, `SoftDeleteItemCommand.cs`, `ItemType.cs`) frontend kodu yazılmadan ÖNCE okunarak üç bug proaktif olarak önlendi (Branch/Warehouse/Contact'taki live-400-sonra-fix döngüsünün aksine):
  - `CreateItemCommand`/`UpdateItemCommand`'da `BranchId` alanı yok (Item tamamen global/branch-agnostic master data) — form hiç böyle bir alan içermiyor.
  - `ItemType` enum'ının gerçekte 4 değeri var (`Inventory/Service/Expense/FixedAsset`), önceki modelde sadece 2 varmış gibi davranılıyordu — düzeltildi, `FixedAsset` seçilince "Faydalı Ömür (Yıl)" alanı koşullu gösteriliyor.
  - `SoftDeleteItemCommand`'ın body alanı `rowVersion` (Base64), önceki `rowVersionBase64` adlandırmasıyla uyuşmuyordu — `items.service.ts`'de düzeltildi.
  - Docker Compose ile canlı doğrulandı: create (`POST /api/items` → 201), edit (`PUT /api/items/{id}` → 200, isim/KDV değişikliği listeye yansıdı), delete (`DELETE /api/items/{id}` → 204, satır listeden kayboldu) — üçü de gerçek tarayıcıda uçtan uca test edildi.
- [x] **Sabit kıymetler (Fixed Assets)** ✅ TAMAMLANDI (2026-08-10) — plandaki "ekranı tamamla" maddesi yanlış varsayıma dayanıyordu: backend bu modülü aylar önce migration `20260118162909_ConsolidateExpensesAndFixedAssetsIntoItems` ile tamamen kaldırmış (`FixedAssets`/`ExpenseDefinitions`/`ExpenseLines`/`ExpenseLists` tabloları drop edilip `Items` tablosuna `UsefulLifeYears`/`PurchaseAccountCode`/`SalesAccountCode` eklenmiş — demirbaş artık sadece `ItemType.FixedAsset` olan bir Item). Backend'de `FixedAssetsController` hiç yok; frontend'deki `/fixed-assets` sayfası hâlâ var olmayan `GET /api/fixed-assets`'e istek atıp canlıda "Hata (404)" veriyordu (doğrulandı). Düzeltme: ölü `fixed-assets` özelliği tamamen kaldırıldı (`fixed-assets-page.component.ts`, `fixed-assets.service.ts`, `fixed-asset.models.ts`, route, sidenav linki) — sabit kıymetler artık zaten tamamlanmış olan Items ekranından (Tür=Demirbaş, Faydalı Ömür alanı) yönetiliyor. `tsc --noEmit` temiz, Docker rebuild sonrası canlıda nav'da "Demirbaşlar" linkinin kaybolduğu ve `/fixed-assets`'in artık login'e yönlendirdiği doğrulandı.
- [x] **Tahsilat/Ödeme (Payments)** ✅ TAMAMLANDI (2026-08-10) — `PaymentFormDialogComponent` (yeni) ile oluştur/düzenle akışı: `EntityPickerComponent` ile Hesap (Kasa/Banka) ve Cari (opsiyonel) seçimi, Tür (Tahsilat/Ödeme), Tarih, Tutar, Para Birimi (TRY/USD/EUR/GBP), Açıklama. Backend kaynağı (`CreatePaymentCommand.cs`, `UpdatePaymentCommand.cs`, `SoftDeletePaymentCommand.cs`, ilgili validator'lar) yazmadan önce okunarak doğru sözleşme çıkarıldı: `UpdatePaymentCommand`'ın RowVersion alanı `rowVersion` (`rowVersionBase64` DEĞİL — frontend modelinde yanlıştı, düzeltildi); `direction` backend'e SAYI (1=In/2=Out) olarak gönderilmeli, ama okuma DTO'ları `direction`'ı zaten string ("In"/"Out") döndürüyor (query handler'da manuel `.ToString()` — asimetrik, converter değil); `currency` backend'de sabit bir whitelist'e (`TRY/USD/EUR/GBP`) karşı doğrulanıyor.
  - **Keşfedilen bağımlılık bug'ı**: Payments formunun hesap seçici picker'ı `CashBankAccountsService`'i kullanıyor, ama o servisin `baseUrl`'i `/cash-bank-accounts` (tire ile) iken backend'in gerçek route'u `[Route("api/[controller]")]` sonucu tiresiz `/api/CashBankAccounts` — her istek 404 veriyordu (canlıda `curl` ile 404 vs 401 karşılaştırmasıyla doğrulandı). Ayrıca `list()` `pageNumber`'ı yanlışlıkla `page` query param'ı olarak gönderiyordu (`ListCashBankAccountsQuery.PageNumber`'a hiç ulaşmıyordu) ve `search` param'ı hiç gönderilmiyordu. Üçü de düzeltildi; ayrıca DTO'larda eksik olan `currency`/`balance` alanları eklendi (backend `CashBankAccountListItemDto`/`DetailDto`'da zaten vardı). Bu servisin create/update/delete tarafındaki ayrı bug'lar (örn. `Code` alanının `CreateCashBankAccountCommand`'da hiç olmaması) Faz 6'daki asıl Kasa/Banka Hesapları ekranı kurulurken ele alınacak — bu oturumda sadece Payments'ın ihtiyaç duyduğu okuma (list/getById) yolu düzeltildi.
  - Docker Compose ile canlı doğrulandı: create (`POST /api/payments` → 201, `{id,amount,currency}` doğru döndü), edit (`PUT /api/payments/{id}` → 200, tutar değişikliği listeye ve silme onay mesajına yansıdı), delete (`DELETE /api/payments/{id}` → 204) — üçü de gerçek tarayıcıda uçtan uca test edildi.

## Faz 6 — Frontend: Kalan Modüller (sıfırdan ekran)

Servis/model katmanı zaten hazır olan, hiç UI'ı olmayan alanlar. Faz 4/5'teki kalıpları (list-grid, form, entity picker) tekrar kullanarak inşa et.

- [x] **Siparişler (Orders)** ✅ TAMAMLANDI (2026-08-11) — `OrdersPageComponent` (liste + şube/durum filtreleri), `OrderFormComponent` (`orders-form.component.ts`, header + AG Grid satır düzenleme, Invoices'ın `invoices-form.component.ts`'ini şablon alıyor ama Order'a özgü basitleştirilmiş: iskonto/tevkifat/belge türü/irsaliye/vade yok, satır başına Net-only hesap), `OrderEditPage` (insert/update/view, route `data.mode`), `OrderActionsCell` (durum bazlı koşullu buton seti: Görüntüle her zaman, Düzenle/Onayla sadece Taslak, İptal Taslak+Onaylandı, Faturaya Dönüştür sadece Onaylandı, Sil Taslak+İptal). Backend kaynağı (`OrdersController.cs`, komutlar/validator'lar/handler'lar, `Order.cs` entity) yazmadan önce okunarak birkaç gerçek gap/sözleşme farkı önceden ele alındı:
  - **Gerçek backend gap'i (düzeltildi)**: `Order.Type` (`InvoiceType` enum'ı yeniden kullanıyor — entity'nin kendi yorumuna göre "as per plan") oluşturmak için zorunlu ama hiçbir read endpoint'i (`OrderListItemDto`, `OrderDetailDto`) döndürmüyordu — yani sipariş oluşturulduktan sonra türünü frontend hiçbir şekilde öğrenemiyordu. `OrderDto.cs`'e her iki DTO'ya da `Type` eklendi, dört handler'da (`ListOrdersHandler`, `GetOrderByIdHandler`, `CreateOrderHandler`, `UpdateOrderHandler`) dolduruldu.
  - **Yetkilendirme boşluğu (düzeltildi)**: `OrdersController`'ın 7 endpoint'inin HİÇBİRİNDE `[Authorize(Policy=...)]` yoktu — sadece genel `RequireAuthorization()` geçerliydi, oysa `Permissions.Order.*` (Read/Create/Update/Delete/Approve/Cancel/CreateInvoice) JWT'de tanımlı ve hiç kontrol edilmiyordu (Stock Transfers'ta Faz 6'nın başında bulunan aynı sınıf boşluk, burada tüm bir controller'ı kapsıyordu). Her action'a karşılık gelen policy eklendi.
  - **Satır hesaplama Invoice'tan farklı**: Order satırının backend'deki tek gerçek alanı `Total` = Net (`Quantity × UnitPrice`, iskonto/tevkifat yok); KDV/Genel Toplam satır bazında backend'den dönmüyor. `money.utils.ts`'teki `calculateInvoiceLine()` (Gross→İskonto→Net→KDV→Tevkifat→GrandTotal zinciri) burada kullanılmadı — `orders-form.component.ts` içinde backend `CreateOrderHandler` ile birebir aynı basit yerel hesap yazıldı (`lineNet = Miktar × BirimFiyat`, `kdv = lineNet × KdvOranı / 100`).
  - **`CreateOrderCommand`/`UpdateOrderCommand` phantom alan temizliği**: Önceden yazılmış `order.models.ts`/`orders.service.ts`, backend'in gerçek command şekliyle (`CreateOrderLineDto`'da `Id` yok, `ItemId` `int?` — yani satırlar ürünsüz serbest metin olabilir; `UpdateOrderLineDto.Id` `int?` — null yeni satır anlamına geliyor; `ListOrdersQuery`'de `Sort`/tarih filtresi yok, sayfalama alanı `Page`) eşleşecek şekilde sıfırdan yeniden yazıldı.
  - **Liste satırında `rowVersion` yok**: `OrderListItemDto` optimistic concurrency token'ı döndürmüyor — Onayla/İptal/Sil öncesi `OrdersPageComponent` önce `getById` ile güncel `rowVersion`'ı çekiyor, sonra ilgili action'ı çağırıyor (Users modülündeki "edit öncesi getById" kalıbının aynısı).
  - Docker Compose ile canlı doğrulandı — tam durum geçiş matrisi test edildi: create (`POST /api/orders` → 201, satır önizleme Net/KDV doğru hesaplandı), approve (`PUT /api/orders/{id}/approve` → Satış tipi sipariş için stok müsaitliği kontrolü dahil, 200), convert-to-invoice (`POST /api/orders/{id}/create-invoice` → 200, oluşan fatura `INV-SO-2026-0005` numarasıyla Faturalar listesinde doğrulandı), cancel (`PUT /api/orders/{id}/cancel` → 200), delete (`DELETE /api/orders/{id}` → 200) — beşi de gerçek tarayıcıda uçtan uca test edildi; her durumda buton setinin (Taslak/Onaylandı/Faturalandı/İptal) doğru koşullu göründüğü doğrulandı.
- [x] **Stok hareketleri (Stock Movements)** ✅ TAMAMLANDI (2026-08-10) — `StockMovementFormDialogComponent` (manuel hareket girişi) + `StockTransferDialogComponent` (depolar arası transfer) + `StockMovementsPageComponent` (salt okunur ledger, +/- işaretli renkli Miktar kolonu). Backend kaynağı (`StockMovementsController.cs`, `StockTransfersController.cs`, `CreateStockMovementCommand.cs`, `TransferStockCommand.cs`, handler'lar) yazmadan önce okunarak birkaç gerçek sözleşme bug'ı önceden düzeltildi:
  - `CreateStockMovementCommand`'da `BranchId` alanı yok (backend `ICurrentUserService.BranchId`'den türetiyor) — frontend modelindeki phantom `branchId` alanı kaldırıldı.
  - `TransferStockCommand`'ın alan adları frontend modelinden tamamen farklıydı: `SourceWarehouseId`/`TargetWarehouseId`/`Description`, frontend'de yanlışlıkla `fromWarehouseId`/`toWarehouseId`/`note` olarak yazılmıştı — düzeltildi.
  - `ListStockMovementsQuery`'nin tarih filtreleri `FromUtc`/`ToUtc` (frontend modelinde yanlışlıkla `dateFromUtc`/`dateToUtc`) — düzeltildi.
  - `StocksService`/`StockMovementsService`'in `list()`'i `pageNumber`'ı yanlışlıkla `page` query param'ı olarak gönderiyordu (Warehouses/Contacts'ta Faz 4'te bulunan aynı bug sınıfı, bu iki serviste daha önce hiç düzeltilmemişti) — düzeltildi.
  - **Route bug'ı (canlıda 404 ile bulundu)**: `StockMovementsController`'ın route'u `[Route("api/[controller]")]` → `/api/StockMovements` (tiresiz), ama `StockMovementsService`'in `baseUrl`'i `/stock-movements` (tireli) idi — CashBankAccounts'ta Faz 5'te bulunan aynı sınıf hata. `StockTransfersController` ise `[Route("api/stock-transfers")]` ile route'u elle tireli tanımlamış — iki controller'ın route stili tutarsız, ikisi de curl ile ayrı ayrı doğrulanarak düzeltildi.
  - **Gerçek backend bug'ı (düzeltildi, UI kapsamının dışında)**: `ListItemsHandler`'ın arama filtresi sadece `Item.Name`'e bakıyordu, `Item.Code`'a hiç bakmıyordu (Stocks/Contacts/CashBankAccounts'un arama filtreleri ikisine de bakıyor) — "LAPTOP" gibi bir SKU ile arama yapıldığında 0 sonuç dönüyordu. Bu, yeni kurulan ürün entity-picker'ının canlı testinde fark edildi (ürün kodu "LAPTOP01" iken arama "LAPTOP" 0 sonuç döndürdü); `ListItemsHandler.cs`'de `Item.Code` de arama koşuluna eklendi.
  - **Yetkilendirme boşluğu (düzeltildi)**: `StockTransfersController.Transfer` endpoint'inde hiç `[Authorize(Policy=...)]` yoktu — sadece genel `RequireAuthorization()` (herhangi bir kimlik doğrulanmış kullanıcı) geçerliydi, oysa JWT'de ayrı bir `Stock.Transfer` permission'ı tanımlı ve hiç kontrol edilmiyordu. `Permissions.Stock.Transfer` policy'si eklendi.
  - Docker Compose ile canlı doğrulandı: manuel hareket girişi (`POST /api/stockmovements` → 201, yeni satır ledger'da `+10.000` yeşil renkte göründü), depolar arası transfer denemesi (seed verisinde her şubede sadece 1 depo olduğu için farklı şube depoları arası transfer denendi, backend'in "Farklı şubeler arası depo transferi (henüz) desteklenmiyor" iş kuralı 422 ile doğru şekilde reddetti — alan adlarının doğru eşleştiğinin kanıtı, çünkü hata 400 validasyon değil 422 iş kuralıydı).
- [x] **Stok durumu (Stocks)** ✅ TAMAMLANDI (2026-08-10) — `StocksPageComponent` (yeni), depo/ürün bazlı salt okunur güncel stok seviyeleri listesi (Depo/Ürün Kodu/Ürün Adı/Birim/Miktar kolonları, depo filtresi + kod/ad arama). Backend'in `ListStocksQuery`'sindeki `BranchId` alanının handler'da hiç kullanılmadığı (branch filtrelemesi `ICurrentUserService` üzerinden otomatik yapılıyor, query param'ı vestigial) not edildi — frontend'de branch filtre UI'ı bilinçli olarak eklenmedi. `StocksService.list()`'in aynı `page`/`pageNumber` bug'ı düzeltildi (yukarıda).
  - Docker Compose ile canlı doğrulandı: liste 57 stok kaydını doğru şekilde (tüm şubeler/depolar, ürün kod/ad/birim/miktar) gösterdi.
- [x] **Kasa/Banka hesapları (Cash & Bank Accounts)** ✅ TAMAMLANDI (2026-08-10) — `CashBankAccountFormDialogComponent` (yeni) ile oluştur/düzenle akışı: create'de Şube seçimi (mat-select, `BranchesService`) + Tür (Kasa/Banka) + Ad + IBAN; edit'te Şube/Kod/Para Birimi alanları backend'de değiştirilemediği için sadece salt-okunur bilgi satırı olarak gösteriliyor (Tür/Ad/IBAN güncellenebilir). Backend kaynağı (`CreateCashBankAccountCommand.cs`, `UpdateCashBankAccountCommand.cs`, handler'lar) yazmadan önce okunarak iki gerçek phantom-alan bug'ı önlendi: `Code` alanı ne create'de ne update'te command'da yok — backend `CBA-{branchId}-{seq}` formatında otomatik üretiyor (`CreateCashBankAccountHandler.GenerateCodeAsync`); `Currency` de create'de yok — entity'de her zaman `"TRY"` default ve API'den hiç değiştirilemiyor. Faz 5'te Payments için düzeltilmiş olan servis bug'larına ek olarak `update()`/`delete()`'in hâlâ yanlış olan `rowVersionBase64` alan adı `rowVersion` olarak düzeltildi (`UpdateCashBankAccountCommand.RowVersion`/`SoftDeleteCashBankAccountCommand.RowVersion` doğrulandı).
  - **Not (seed data, kod bug'ı değil)**: seed'deki "BANKA01"/"BANKA02" kodlu hesaplar backend'de `Type: "Cash"` olarak seed edilmiş (muhtemelen kopyala-yapıştır hatası) — ekran bunu doğru yansıtıyor, düzeltme kapsamı dışında bırakıldı.
  - Docker Compose ile canlı doğrulandı: create (`POST /api/cashbankaccounts` → 201, kod `CBA-1-00001` olarak otomatik üretildi), edit (`PUT /api/cashbankaccounts/{id}` → 200, Tür Kasa→Banka değişikliği listeye yansıdı), delete (`DELETE /api/cashbankaccounts/{id}` → 204) — üçü de gerçek tarayıcıda uçtan uca test edildi.
- [ ] **Çek/Senet (Cheques)**: liste + al/ver/tahsil et/ciro et akışları (backend `Receive`/`Issue`/`Cash`/`Endorse` command'larını kapsayacak şekilde).
- [x] **Kategoriler (Categories)** ✅ TAMAMLANDI (2026-08-10) — `CategoriesService`/`CategoryFormDialogComponent`/`CategoriesPageComponent` (hepsi yeni) ile liste + oluştur/düzenle/sil. Backend kaynağı (`CategoriesController.cs`, komutlar/validator'lar) yazmadan önce okunarak iki modüle özgü sözleşme farkı keşfedildi ve önceden ele alındı:
  - **Gerçek backend gap'i (düzeltildi)**: `CategoriesController`'da hiç `GetById` endpoint'i yok, ve edit/delete `RowVersion` gerektiriyor — ama `CategoryListItemDto`'da `RowVersion` alanı yoktu, yani frontend'in edit/delete için RowVersion'ı ELDE ETMENİN HİÇBİR YOLU yoktu. Backend'de `CategoryDto.cs`'e `RowVersion` eklendi ve `ListCategoriesHandler.cs`'de dolduruldu (yeni bir GetById endpoint'i eklemek yerine — Category'nin zaten tüm alanları listede, ayrı bir "detail" kavramına ihtiyaç yok). Bu sayede liste satırı doğrudan edit/delete'e (getById çağrısı olmadan) besleniyor.
  - Diğer modüllerden farklı iki sözleşme tuhaflığı doğru şekilde ele alındı: sayfalama parametresi `pageNumber` değil `page` (`ListCategoriesQuery.Page`); `DELETE`'in `RowVersion`'ı body'de değil **query string**'de bekleniyor (`[FromQuery] string rowVersion`) — `CategoriesService.delete()` `HttpParams` ile gönderiyor, diğer modüllerdeki body-tabanlı delete'lerden farklı.
  - Docker Compose ile canlı doğrulandı: create (`POST /api/categories` → 200, backend `Ok()` döndürüyor — `Created` değil), edit (`PUT /api/categories/{id}` → 200, açıklama değişikliği listeye yansıdı), delete (`DELETE /api/categories/{id}?rowVersion=...` → 200, satır listeden kayboldu) — üçü de gerçek tarayıcıda uçtan uca test edildi.
- [x] **Firma ayarları (Company Settings)** ✅ TAMAMLANDI (2026-08-10) — `CompanySettingsPageComponent` (yeni) tekil kayıt formu: `ngOnInit`'te `GET /api/company-settings` ile mevcut değerler yüklenip forma basılıyor, kaydet `PUT`'un döndürdüğü güncel `rowVersionBase64`'ü component state'inde saklıyor (sayfa yenilemeden art arda kayıt yapılabiliyor — model/servis zaten önceki bir oturumda doğru yazılmıştı, sadece ekran eksikti; `CompanySettingsService.update()`'in dönüş tipi `void`'den güncel DTO'ya düzeltildi ki bu RowVersion-yenileme akışı çalışsın).
  - Docker Compose ile canlı doğrulandı: form seed verisiyle (`Demo Şirketi A.Ş.` vb.) dolu geldi, telefon alanı değiştirilip kaydedildi (`PUT` → 200, response'ta yeni değer + yeni RowVersion), sayfa yenilenmeden ikinci bir kayıt (eski değere geri alma) de RowVersion çakışması olmadan başarılı oldu — optimistic concurrency akışı uçtan uca doğrulandı.
- [x] **Kullanıcı yönetimi (Users)** ✅ TAMAMLANDI (2026-08-10) — `UsersPageComponent` (liste + filtreler: şube/rol/durum/arama), `UserFormDialogComponent` (oluştur/düzenle — düzenlemede şifre alanı gizli), `ChangePasswordDialogComponent`, `UserActionsCell` (Düzenle/Şifre Değiştir/Sil — `EntityActionsCell`'e üçüncü buton eklemek yerine bu ekrana özel yeni bir cell renderer yazıldı). Rotalar `adminGuard`'a bağlandı (Faz 3'te "guard hazır ama bağlanacak route yok" notuyla ertelenmişti — artık gerçek admin-only ekranlar var); sidenav'da "Kullanıcılar"/"Roller" linkleri sadece `isAdmin()` true iken gösteriliyor.
  - **Gerçek backend gap'i (düzeltildi) — şifre değiştirme özelliği hiç yoktu**: TASKS.md bu maddeyi "+ şifre değiştirme (admin-only)" olarak tanımlıyordu, ama backend'de böyle bir command/endpoint yoktu — `UsersController`'da sadece Create/Update/Delete vardı, `AuthController`'da da self-servis bir "mevcut şifreyi değiştir" akışı yoktu. Yeni `ChangeUserPasswordCommand`/`Validator`/`Handler` + `POST /api/users/{id}/change-password` (mevcut `IPasswordHasher`/`Permissions.User.Update` policy'si yeniden kullanıldı) eklendi. Frontend'in önceden yazılmış `ChangePasswordBody.currentPassword` alanı da hayali bir sözleşmeydi (backend hiç böyle bir kontrol yapmıyor, bu admin'in başkasının şifresini resetlemesi akışı) — kaldırıldı.
  - **Sözleşme bug'ları (düzeltildi)**: `ListUsersQuery`'nin sayfalama alanı `Page` (`pageNumber` değil — Categories'teki gibi); frontend `pageNumber` gönderiyordu, düzeltildi. `SoftDeleteUserCommand`/`UpdateUserCommand`'da RowVersion YOK (bu modülde optimistic concurrency hiç kullanılmıyor) — frontend zaten doğru yazılmıştı, ekstra kontrol gerekmedi.
  - **Rol adı → Rol ID eşleştirmesi**: `UserDetailDto.Roles` backend'den rol İSİMLERİ (string) döner, ama `UpdateUserCommand.RoleIds` sayısal ID listesi bekler — form dialog, önceden yüklenmiş roller listesiyle isim eşleştirmesi yaparak (`data.roles.filter(r => names.has(r.name))`) başlangıç seçili ID'lerini türetiyor.
  - Docker Compose ile canlı doğrulandı: create (`POST /api/users` → 201), edit (`PUT /api/users/{id}` → 204, ad değişikliği listeye yansıdı), **şifre değiştirme uçtan uca gerçek login ile doğrulandı** (`POST /api/users/{id}/change-password` → 204, ardından yeni şifreyle `POST /api/auth/login` → 200 ve geçerli JWT döndü), delete (`DELETE /api/users/{id}` → 204) — dördü de gerçek tarayıcıda/curl'de test edildi.
- [x] **Rol yönetimi (Roles)** ✅ TAMAMLANDI (2026-08-10) — `RolesPageComponent` (liste) + `RoleFormDialogComponent` (oluştur/düzenle, gruplandırılmış izin checkbox'ları — modül başlığına tıklayınca o modülün tüm izinleri seçilir/kaldırılır, kısmi seçimde checkbox indeterminate durumuna geçer).
  - **Sözleşme bug'ı (düzeltildi) — liste sayfalama değil düz dizi**: `ListRolesQuery`'nin backend'de HİÇBİR parametresi yok (`record ListRolesQuery : IRequest<List<RoleListItemDto>>`) ve handler `PagedResult` değil düz `List<RoleListItemDto>` döndürüyor — ama önceden yazılmış frontend `RolesService.list()` `PagedResult<RoleListItemDto>` bekleyip `pageNumber`/`pageSize`/`search`/`sort` gönderiyordu (hiçbiri backend'de işlenmiyor). Servis düzeltildi (`list(): Observable<RoleListItemDto[]>`, parametresiz); `RolesPageComponent`'in `fetcher`'ı `ListGridComponent`'in beklediği `{items, total}` şekline `map()` ile sarıyor.
  - **İzin kataloğu backend'den gelmiyor**: `CreateRoleCommand`/`UpdateRoleCommand.Permissions` düz `string[]` (izin adı) bekliyor, ama izinlerin tam listesini döndüren bir API yok — sadece `Permissions.cs`'in sunucu tarafı sabiti var. `role.models.ts`'e `PERMISSION_GROUPS` sabiti eklendi, `Permissions.GetAll()`'daki 16 modül/62 izinle birebir eşleşecek şekilde elle yazıldı (senkron tutulması gerekiyor — backend'e yeni izin eklenirse bu dosya da güncellenmeli).
  - Docker Compose ile canlı doğrulandı: create (`POST /api/roles` → 201, modül-bazlı toplu seçim + tekil seçim karışık test edildi — "Cariler" grubu tek tıkla 4 izin seçti + `Item.Read` tekil eklendi, "5 seçili" doğru sayıldı, kaydedilen rolde `permissionCount: 5` göründü), delete (`DELETE /api/roles/{id}` → 204).

## Faz 7 — Dashboard & Raporlama UI

- [ ] Ana sayfa/dashboard ekranı (root route artık `/invoices`'a değil `/dashboard`'a yönlensin) — backend'deki `GET /api/reports/dashboard` endpoint'ini kullanan özet kartlar (günlük satış, kasa durumu, alacaklar vb.).
- [ ] Cari ekstre ekranı (`GET /api/reports/contact/{id}/statement`).
- [ ] Stok durumu raporu ekranı + Excel export (backend zaten `stock-status/export` sağlıyor).
- [ ] Gelir/gider raporu ekranı (`income-expense` endpoint'i).

## Faz 8 — Rol/Yetki Bazlı UI, Tutarlılık, Cilalama

- [ ] Backend'in `permission` claim listesine göre çalışan bir permission-check servis/directive'i yaz (`hasPermission('Invoice.Create')` gibi); menüdeki her linki ve her oluştur/düzenle/sil butonunu buna göre koşullu göster.
- [ ] Sidenav menüsünü role/permission'a göre filtrele (şu an her authenticated kullanıcıya aynı 4 link gösteriliyor).
- [ ] Silme işlemleri için tutarlı bir onay dialog'u (MatDialog) pattern'i kur, tüm liste ekranlarına uygula.
- [ ] Boş durum (empty state) ve hata durumu gösterimlerini tüm liste ekranlarında tutarlı hale getir.
- [ ] Tüm yeni ekranların `FRONTEND_RULES.md`'deki money/tarih/route/naming kurallarına uyduğunu gözden geçir.

## Faz 9 — Test, CI, Deploy

- [ ] Kritik akışlar için gerçek Angular unit/component testleri yaz (auth flow, invoice form hesaplama mantığı, guard'lar) — şu an tek spec dosyası bozuk ve gerçek kapsam sıfır.
- [ ] Backend + frontend için GitHub Actions CI pipeline'ı kur (build + test her push/PR'da) — ECommercePlatform projesindeki `.github/workflows/ci.yml` deseniyle tutarlı.
- [ ] `docker-compose.yml`'i gözden geçir: Faz 0'daki seed/migration guard'ını yansıt, backend Dockerfile'a non-root user + healthcheck ekle.
- [ ] Projeyi bir sunucuya canlıya al (ECommercePlatform/WMS'teki gibi kendi subdomain'i ile), portföydeki proje kartına canlı demo linki ekle.
- [ ] Son bir uçtan uca manuel tur: login → cari oluştur → stok kartı oluştur → sipariş oluştur → onayla → faturaya çevir → tahsilat işle → dashboard'da rakamların doğru yansıdığını doğrula.

---

## İlerlemeyi nasıl kullanmalı

Bu liste kabaca öncelik sırasına göredir ama katı değildir — Faz 0 kesinlikle önce gelmeli (güvenlik), Faz 4 Faz 5/6'dan önce gelmeli (bağımlılık), gerisi paralel ilerlenebilir. Her fazı bitirdikçe ilgili kutucukları işaretle; büyük/çok parçalı maddeler tamamlandıkça alt görevlere bölünebilir.
